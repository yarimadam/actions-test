name: build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    container:
      image: yarimadam/php

    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Run Check
        run: php check.php
