name: build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    container:
      image: docker://yarimadam/php

    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Use Docker
        uses: docker://yarimadam/php

      - name: Run Check
        run: php check.php